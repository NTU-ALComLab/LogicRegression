#ifndef INFO_H
#define INFO_H

struct CubeInfo {
    bool isConverged;
    bool cubeTendency;
    bool posCofacTendency;
    bool negCofacTendency;
    bool isXor;
    int criticalInput;
};

#endif // INFO_H
